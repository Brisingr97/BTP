import numpy as np
import cv2
from matplotlib import pyplot as plt
import sys
import os

def import_image(path):
    pass

def find_corner_points(image_object):
    return_list_of_coordinates = []
    return return_list_of_coordinates

def find_r_theta(point_coordinate,centre_coordinate):
    r = 0;
    o = 0;
    return(r,o)
def vote():
    # initiate vote matrix
    # for all points find the centre and then find the theta and the r
    
    pass
